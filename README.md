# Native Downloads Watcher (Torrent Auto-Seeder)

A lightweight native Bash script that watches your `~/Downloads` folder for new `.torrent` files and automatically moves them into a Transmission watch/seeding directory.

This setup is fully dependency-free and uses only built-in shell tools (`bash`, `stat`, `ls`, `mv`, `open`).
